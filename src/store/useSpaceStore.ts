import { create } from 'zustand';
import type { OmniTab, Space } from '../types/omniTab';
interface SpaceStore { spaces: Space[]; activeSpaceId: string|null; omniTabs: OmniTab[]; activeOmniTabId: string|null; setActiveTab(id:string):void; openTab(tab:Omit<OmniTab,'id'>):string; closeTab(id:string):void; moveTab(f:number,t:number):void; updateTabLabel(id:string,url:string,title:string):void; createSpace(name:string,agentIds?:string[]):Space; deleteSpace(id:string):void; updateSpace(id:string,patch:Partial<Space>):void; setActiveSpaceId(id:string|null):void; hydrate():Promise<void>; persist():Promise<void>; }
export const useSpaceStore = create<SpaceStore>(() => ({ spaces:[], activeSpaceId:null, omniTabs:[], activeOmniTabId:null, setActiveTab:()=>{}, openTab:()=>'', closeTab:()=>{}, moveTab:()=>{}, updateTabLabel:()=>{}, createSpace:()=>({id:'',name:'',agentIds:[],peopleIds:[],tabIds:[],createdAt:0,updatedAt:0}), deleteSpace:()=>{}, updateSpace:()=>{}, setActiveSpaceId:()=>{}, hydrate:async()=>{}, persist:async()=>{} }));
