// Stub — full implementation in Unit 4 PR
import React from 'react';
export function BrowserTabContent(_props: { tabId: string; initialUrl?: string }): React.JSX.Element {
  return <div className="flex-1 bg-[#0a0b0e] flex items-center justify-center text-neutral-500 text-sm">Browser Tab</div>;
}
