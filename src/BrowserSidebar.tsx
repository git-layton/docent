// Temporary stub — replaced by Unit 3
export function BrowserSidebar({ url: _url, pageTitle: _pageTitle, pageContent: _pageContent }: { url: string; pageTitle: string; pageContent: string }) {
  return null;
}
