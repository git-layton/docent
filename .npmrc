# @emoji-mart/react@1.1.1 declares a React <=18 peer range, but the app runs React 19
# (and works fine against it). Accept the peer mismatch so `npm install`, `npm ci`, and
# CI all resolve the lockfile identically and reproducibly.
legacy-peer-deps=true
